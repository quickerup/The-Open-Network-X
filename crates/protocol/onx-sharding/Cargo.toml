[package]
name = "onx-sharding"
version = "0.1.0"
edition = "2021"
description = "ONX dynamic sharding, shard tree invariants, split/merge triggers, state migration, and routing continuity per docs/specification/sharding.md"
license = "Apache-2.0"
publish = false

[dependencies]
onx-primitives = { path = "../onx-primitives" }
onx-data-structures = { path = "../onx-data-structures" }
onx-state-model = { path = "../onx-state-model" }
onx-blocks = { path = "../onx-blocks" }

[dev-dependencies]
hex = "0.4"
